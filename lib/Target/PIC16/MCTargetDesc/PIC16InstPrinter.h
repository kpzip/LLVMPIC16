//===- PIC16InstPrinter.h - Convert PIC16 MCInst to assembly syntax -*- C++ -*-===//
//
// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
// See https://llvm.org/LICENSE.txt for license information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
//===----------------------------------------------------------------------===//
//
// This class prints an PIC16 MCInst to a .s file.
//
//===----------------------------------------------------------------------===//

#ifndef LLVM_PIC16_INST_PRINTER_H
#define LLVM_PIC16_INST_PRINTER_H

#include "llvm/MC/MCInstPrinter.h"

//#include "MCTargetDesc/PIC16MCTargetDesc.h"

namespace llvm {

/// Prints PIC16 instructions to a textual stream.
class PIC16InstPrinter : public MCInstPrinter {
public:
  PIC16InstPrinter(const MCAsmInfo &MAI, const MCInstrInfo &MII,
                 const MCRegisterInfo &MRI)
      : MCInstPrinter(MAI, MII, MRI) {}

  static const char *getPrettyRegisterName(unsigned RegNo,
                                           MCRegisterInfo const &MRI);

  void printInst(const MCInst *MI, uint64_t Address, StringRef Annot,
                 const MCSubtargetInfo &STI, raw_ostream &O) override;

private:
  static const char *getRegisterName(MCRegister Reg/*,
                                     unsigned AltIdx = /*PIC16::NoRegAltName 0*/);

  void printOperand(const MCInst *MI, unsigned OpNo, raw_ostream &O);
  void printCCOperand(const MCInst *MI, unsigned OpNo, raw_ostream &O);
  void printPCRelImm(const MCInst *MI, unsigned OpNo, raw_ostream &O);
  void printPCRelImm(const MCInst *MI, uint64_t /*Address*/, unsigned OpNo,
                     raw_ostream &O) {
    printPCRelImm(MI, OpNo, O);
  }
  void printMemri(const MCInst *MI, unsigned OpNo, raw_ostream &O);
  void printMemspi(const MCInst *MI, unsigned OpNo, raw_ostream &O) {
    printMemri(MI, OpNo, O);
  }

  // Autogenerated by TableGen.
  std::pair<const char *, uint64_t> getMnemonic(const MCInst *MI) override;
  void printInstruction(const MCInst *MI, uint64_t Address, raw_ostream &O);
  bool printAliasInstr(const MCInst *MI, uint64_t Address, raw_ostream &O);
  void printCustomAliasOperand(const MCInst *MI, uint64_t Address,
                               unsigned OpIdx, unsigned PrintMethodIdx,
                               raw_ostream &O);
};

} // end namespace llvm

#endif // LLVM_PIC16_INST_PRINTER_H
